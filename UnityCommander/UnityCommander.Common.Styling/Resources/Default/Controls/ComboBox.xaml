<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="UcComboBoxBackgroundBrush" Color="{DynamicResource ComboBox.Background.Color}" />

    <SolidColorBrush x:Key="UcComboBoxBackgroundHoverBrush" Color="{DynamicResource ComboBox.Background.Hover.Color}" />

    <SolidColorBrush x:Key="UcComboBoxBackgroundPressedBrush" Color="{DynamicResource ComboBox.Background.Pressed.Color}" />

    <SolidColorBrush x:Key="UcComboBoxBorderBrush" Color="{DynamicResource ComboBox.Border.Color}" />

    <SolidColorBrush x:Key="UcComboBoxBorderHoverBrush" Color="{DynamicResource ComboBox.Border.Hover.Color}" />

    <SolidColorBrush x:Key="UcComboBoxBorderPressedBrush" Color="{DynamicResource ComboBox.Border.Pressed.Color}" />

    <SolidColorBrush x:Key="UcComboBoxForegroundBrush" Color="{DynamicResource ComboBox.Foreground.Color}" />

    <SolidColorBrush x:Key="UcComboBoxArrowBrush" Color="{DynamicResource ComboBox.Arrow.Color}" />

    <SolidColorBrush x:Key="UcComboBoxArrowHoverBrush" Color="{DynamicResource ComboBox.Arrow.Hover.Color}" />

    <SolidColorBrush x:Key="UcComboBoxPopupBackgroundBrush" Color="{DynamicResource ComboBox.Popup.Background.Color}" />

    <SolidColorBrush x:Key="UcComboBoxPopupBorderBrush" Color="{DynamicResource ComboBox.Popup.Border.Color}" />

    <SolidColorBrush x:Key="UcComboBoxItemForegroundBrush" Color="{DynamicResource ComboBox.Item.Foreground.Color}" />

    <SolidColorBrush x:Key="UcComboBoxItemHoverBrush" Color="{DynamicResource ComboBox.Item.Background.Hover.Color}" />

    <SolidColorBrush x:Key="UcComboBoxItemSelectedBrush" Color="{DynamicResource ComboBox.Item.Background.Selected.Color}" />


    <Style x:Key="UcComboBoxStyle" TargetType="{x:Type ComboBox}">

        <Setter Property="Height" Value="28" />
        <Setter Property="MinWidth" Value="150" />

        <Setter Property="Background" Value="{StaticResource UcComboBoxBackgroundBrush}" />

        <Setter Property="BorderBrush" Value="{StaticResource UcComboBoxBorderBrush}" />

        <Setter Property="BorderThickness" Value="1" />

        <Setter Property="Foreground" Value="{StaticResource UcComboBoxForegroundBrush}" />

        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />

        <Setter Property="Template">
            <Setter.Value>

                <ControlTemplate TargetType="{x:Type ComboBox}">

                    <Grid>

                        <!--  Main button  -->
                        <ToggleButton
                            x:Name="DropDownButton"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            ClickMode="Press"
                            Focusable="False"
                            IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">

                            <ToggleButton.Template>
                                <ControlTemplate TargetType="{x:Type ToggleButton}">

                                    <Border
                                        x:Name="ButtonBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="5">

                                        <Grid>

                                            <!--  Selected value  -->
                                            <ContentPresenter
                                                Margin="9,0,30,0"
                                                HorizontalAlignment="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                VerticalAlignment="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                ContentStringFormat="{Binding SelectionBoxItemStringFormat, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                ContentTemplateSelector="{Binding ItemTemplateSelector, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                IsHitTestVisible="False"
                                                SnapsToDevicePixels="{Binding SnapsToDevicePixels, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                TextElement.Foreground="{StaticResource UcComboBoxForegroundBrush}" />
                                            <!--  Arrow  -->
                                            <Path
                                                x:Name="Arrow"
                                                Width="8"
                                                Height="5"
                                                Margin="0,0,10,0"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Data="M 0,0 L 4,4 L 8,0"
                                                Fill="{DynamicResource UcComboBoxArrowBrush}"
                                                Stretch="None" />

                                        </Grid>

                                    </Border>

                                    <ControlTemplate.Triggers>

                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource UcComboBoxBackgroundHoverBrush}" />

                                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource UcComboBoxBorderHoverBrush}" />

                                            <Setter TargetName="Arrow" Property="Fill" Value="{StaticResource UcComboBoxArrowHoverBrush}" />
                                        </Trigger>

                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource UcComboBoxBackgroundPressedBrush}" />

                                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource UcComboBoxItemSelectedBrush}" />
                                        </Trigger>

                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource UcComboBoxItemHoverBrush}" />

                                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource UcComboBoxItemHoverBrush}" />

                                            <Setter TargetName="Arrow" Property="Data" Value="M 0,4 L 4,0 L 8,4" />

                                            <Setter TargetName="Arrow" Property="Fill" Value="{StaticResource UcComboBoxArrowHoverBrush}" />
                                        </Trigger>

                                    </ControlTemplate.Triggers>

                                </ControlTemplate>
                            </ToggleButton.Template>

                        </ToggleButton>


                        <!--  Popup  -->
                        <Popup
                            x:Name="PART_Popup"
                            AllowsTransparency="True"
                            Focusable="False"
                            IsOpen="{TemplateBinding IsDropDownOpen}"
                            Placement="Bottom"
                            PlacementTarget="{Binding ElementName=DropDownButton}"
                            PopupAnimation="Fade">

                            <Border
                                MinWidth="{Binding ActualWidth, ElementName=DropDownButton}"
                                Margin="0,3,0,0"
                                Padding="3"
                                Background="{StaticResource UcComboBoxPopupBackgroundBrush}"
                                BorderBrush="{StaticResource UcComboBoxPopupBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="5">

                                <ScrollViewer
                                    CanContentScroll="True"
                                    HorizontalScrollBarVisibility="Disabled"
                                    VerticalScrollBarVisibility="Auto">

                                    <ItemsPresenter />

                                </ScrollViewer>

                            </Border>

                        </Popup>

                    </Grid>

                </ControlTemplate>

            </Setter.Value>
        </Setter>

    </Style>

    <Style x:Key="UcComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">

        <Setter Property="Height" Value="30" />
        <Setter Property="Padding" Value="8,0" />

        <Setter Property="Foreground" Value="{DynamicResource UcComboBoxItemForegroundBrush}" />

        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />

        <Setter Property="Template">
            <Setter.Value>

                <ControlTemplate TargetType="{x:Type ComboBoxItem}">

                    <Border
                        x:Name="ItemBorder"
                        Margin="1"
                        Padding="{TemplateBinding Padding}"
                        Background="Transparent"
                        CornerRadius="4">

                        <ContentPresenter
                            Margin="3"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />

                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">

                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource UcComboBoxItemHoverBrush}" />

                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">

                            <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource UcComboBoxItemSelectedBrush}" />

                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>

            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>