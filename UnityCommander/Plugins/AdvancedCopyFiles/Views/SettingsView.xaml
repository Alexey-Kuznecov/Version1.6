<UserControl
    x:Class="AdvancedCopyFiles.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AdvancedCopyFiles.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <ScrollViewer>
            <StackPanel Margin="10">
                <TextBlock Text="Источник:" />
                <TextBox Text="{Binding SourcePath}" />

                <TextBlock Margin="0,10,0,0" Text="Назначение:" />
                <TextBox Text="{Binding DestinationPath}" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <CheckBox Content="Многопоточное копирование" IsChecked="{Binding UseMultiThreading}" />
                    <Label Grid.Column="1" Padding="10,0,10,0">Кол-во потоков</Label>
                    <TextBox
                        Grid.Column="2"
                        Padding="10,0,10,0"
                        Text="{Binding MaxConcurrentTasks}" />
                </Grid>
                <CheckBox Content="Копировать скрытые" IsChecked="{Binding CopyHidden}" />
                <CheckBox Content="Копировать системные" IsChecked="{Binding CopySystem}" />
                <CheckBox Content="Копировать архивные" IsChecked="{Binding CopyArchive}" />

                <!--  Фильтры  -->
                <TextBlock Margin="0,10,0,0" Text="Фильтры (регулярка, расширения):" />
                <TextBox Text="{Binding FilterPattern}" />

                <!--  Дата  -->
                <GroupBox Margin="0,10,0,0" Header="Фильтрация по дате изменения">
                    <StackPanel Margin="0,5,0,0" Orientation="Horizontal">
                        <TextBlock Width="50" Text="От:" />
                        <DatePicker Width="150" SelectedDate="{Binding DateFrom}" />
                        <TextBlock
                            Width="50"
                            Margin="20,0,0,0"
                            Text="До:" />
                        <DatePicker Width="150" SelectedDate="{Binding DateTo}" />
                    </StackPanel>
                </GroupBox>

                <!--  Размер  -->
                <GroupBox Margin="0,10,0,0" Header="Фильтрация по размеру файлов">
                    <StackPanel>
                        <StackPanel Margin="0,5,0,0" Orientation="Horizontal">
                            <TextBlock Width="100" Text="Больше чем:" />
                            <TextBox Width="100" Text="{Binding MinFileSize}" />
                            <ComboBox
                                Width="70"
                                Margin="10,0,0,0"
                                SelectedItem="{Binding MinFileSizeUnit}">
                                <ComboBoxItem Content="B" />
                                <ComboBoxItem Content="KB" />
                                <ComboBoxItem Content="MB" />
                                <ComboBoxItem Content="GB" />
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Margin="5,5,0,0" Orientation="Horizontal">
                            <TextBlock Width="100" Text="Меньше чем:" />
                            <TextBox Width="100" Text="{Binding MaxFileSize}" />
                            <ComboBox
                                Width="70"
                                Margin="10,0,0,0"
                                SelectedItem="{Binding MaxFileSizeUnit}">
                                <ComboBoxItem Content="B" />
                                <ComboBoxItem Content="KB" />
                                <ComboBoxItem Content="MB" />
                                <ComboBoxItem Content="GB" />
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <!--  Поведение при конфликте  -->
                <TextBlock Margin="0,10,0,0" Text="Поведение при конфликте:" />
                <ComboBox SelectedItem="{Binding ConflictBehavior}">
                    <ComboBoxItem Content="Перезаписывать" />
                    <ComboBoxItem Content="Пропускать" />
                    <ComboBoxItem Content="Переименовывать" />
                    <ComboBoxItem Content="Спрашивать" />
                </ComboBox>

                <!--  Профили  -->
                <TextBlock Margin="0,10,0,0" Text="Профили:" />
                <StackPanel Orientation="Horizontal">
                    <ComboBox
                        Width="200"
                        ItemsSource="{Binding Profiles}"
                        SelectedItem="{Binding SelectedProfile}" />
                    <Button Margin="5,0,0,0" Content="Сохранить" />
                    <Button Margin="5,0,0,0" Content="Загрузить" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
