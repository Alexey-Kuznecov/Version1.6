<UserControl
    x:Class="AdvancedCopyFiles.Views.FileListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:AdvancedCopyFiles.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AdvancedCopyFiles.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:session="clr-namespace:UnityCommander.Copying.Sessions;assembly=UnityCommander.Copying"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <!--<UserControl.Resources>
        <converters:StatusColorConverter x:Key="StatusColorConverter" />
    </UserControl.Resources>-->

    <Grid>
        <DockPanel Margin="5">
            <!--  Панель фильтров  -->
            <StackPanel
                Margin="0,0,0,5"
                DockPanel.Dock="Top"
                Orientation="Horizontal">
                <TextBlock
                    Margin="0,0,5,0"
                    VerticalAlignment="Center"
                    Text="Показать:" />
                <ComboBox
                    Width="200"
                    Margin="0,0,10,0"
                    SelectedItem="{Binding SelectedFileFilter}">
                    <ComboBoxItem Content="Все файлы" />
                    <ComboBoxItem Content="Пропущенные" />
                    <ComboBoxItem Content="Скопированные" />
                    <ComboBoxItem Content="С ошибкой" />
                    <ComboBoxItem Content="В процессе" />
                </ComboBox>

                <TextBlock
                    Margin="0,0,5,0"
                    VerticalAlignment="Center"
                    Text="Поиск:" />
                <TextBox Width="200" Text="{Binding FileSearchText, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <!--  Быстрая версия списка файлов  -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding FilteredFiles}">
                    <ItemsControl.ItemsPanel>
                        <!--  Виртуализирующий стек  -->
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel
                                IsVirtualizing="True"
                                ScrollUnit="Pixel"
                                VirtualizationMode="Recycling" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type session:FileCopyItem}">
                            <Border
                                Padding="4"
                                BorderBrush="#DDD"
                                BorderThickness="0,0,0,1">
                                <Grid Margin="4,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="3*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding FileName}" />
                                    <TextBlock Grid.Column="1" Text="{Binding Source}" />
                                    <TextBlock Grid.Column="2" Text="{Binding BytesCopiedText}" />
                                    <TextBlock Grid.Column="3" Text="{Binding FileSizeText}" />

                                    <!--  Прогресс  -->
                                    <ProgressBar
                                        Grid.Column="4"
                                        Height="16"
                                        Margin="5,0"
                                        VerticalAlignment="Center"
                                        Maximum="100"
                                        Minimum="0"
                                        Value="{Binding Progress}" />

                                    <!--<TextBlock
                                        Grid.Column="5"
                                        Margin="5,0,0,0"
                                        Foreground="{Binding Status, Converter={StaticResource StatusColorConverter}}"
                                        Text="{Binding Status}" />-->
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>
