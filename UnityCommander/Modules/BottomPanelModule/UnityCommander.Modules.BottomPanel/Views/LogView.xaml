<UserControl
    x:Class="UnityCommander.Modules.BottomPanel.Views.LogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:UnityCommander.Modules.BottomPanel.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:UnityCommander.Modules.BottomPanel.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="250"
    mc:Ignorable="d">

    <UserControl.Resources>
        <conv:LogColorToBrushConverter x:Key="LogColorConverter" />
        <conv:BoolToVisibilityConverter x:Key="VisibilityConverter" />
    </UserControl.Resources>
    <UserControl.InputBindings>
        <KeyBinding
            Key="E"
            Command="{Binding ToggleEditModeCommand}"
            Modifiers="Ctrl" />
        <KeyBinding
            Key="D"
            Command="{Binding ClearCommand}"
            Modifiers="Alt" />
    </UserControl.InputBindings>
    <Grid Background="#1E1E1E">
        <!--  Лог: TextBox  -->
        <TextBox
            x:Name="TextLog"
            AcceptsReturn="True"
            Background="#1E1E1E"
            FontFamily="Consolas"
            FontSize="12"
            Foreground="White"
            IsReadOnly="True"
            Text="{Binding LogText}"
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto"
            Visibility="{Binding IsHighlightMode, Converter={StaticResource VisibilityConverter}, ConverterParameter=False}" />

        <!--  Лог: ListBox с подсветкой  -->
        <ListBox
            x:Name="LogList"
            Padding="0"
            Background="#1E1E1E"
            BorderThickness="0"

            ItemsSource="{Binding Logs}"
            Loaded="OnLoaded"
            ScrollViewer.CanContentScroll="True"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            VirtualizingStackPanel.IsVirtualizing="True"
            VirtualizingStackPanel.VirtualizationMode="Recycling"
            Visibility="{Binding IsHighlightMode, Converter={StaticResource VisibilityConverter}}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Focusable" Value="True" />
                    <Setter Property="IsHitTestVisible" Value="True" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <ItemsControl ItemsSource="{Binding Parts}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock
                                    FontFamily="Consolas"
                                    FontSize="12"
                                    Foreground="{Binding Color}"
                                    Text="{Binding Text}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--  Кнопка поверх лога  -->
        <Button
            Margin="5"
            Padding="2,1"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Background="Transparent"
            BorderThickness="0"
            Command="{Binding ToggleEditModeCommand}"
            Content="{Binding EditModeText}"
            FontWeight="Bold"
            Foreground="White"
            Opacity="0.7" />
    </Grid>
</UserControl>
