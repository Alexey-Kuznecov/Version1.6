<UserControl
    x:Class="UnityCommander.Modules.BottomPanel.Views.ConsoleActivityWidget"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:UnityCommander.Modules.BottomPanel.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid x:Name="WidgetGrid">
        <Border
            Padding="12,9"
            Background="#CC181818"
            BorderBrush="#35FFFFFF"
            BorderThickness="1"
            CornerRadius="8">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Header  -->
                <Grid Grid.Row="0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        FontSize="13"
                        FontWeight="SemiBold"
                        Foreground="#F5F5F5"
                        Text="{Binding Title}" />

                    <Button
                        x:Name="CollapseButton"
                        Grid.Column="1"
                        Width="24"
                        Height="24"
                        Margin="10,0,0,0"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="ToggleExpanded"
                        Content="˄"
                        FontSize="14"
                        Foreground="#BFFFFFFF" />

                </Grid>

                <!--  Details  -->
                <Grid
                    x:Name="Details"
                    Grid.Row="1"
                    Margin="0,10,0,0">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Foreground="#99FFFFFF"
                        Text="Found" />

                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="2"
                        HorizontalAlignment="Right"
                        Foreground="#FFFFFF"
                        Text="{Binding Found}" />


                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Margin="0,5,0,0"
                        Foreground="#99FFFFFF"
                        Text="Processed" />

                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="2"
                        Margin="0,5,0,0"
                        HorizontalAlignment="Right"
                        Foreground="#FFFFFF"
                        Text="{Binding Processed}" />


                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="0"
                        Margin="0,5,0,0"
                        Foreground="#99FFFFFF"
                        Text="Skipped" />

                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="2"
                        Margin="0,5,0,0"
                        HorizontalAlignment="Right"
                        Foreground="#FFFFFF"
                        Text="{Binding Skipped}" />


                    <TextBlock
                        Grid.Row="3"
                        Grid.Column="0"
                        Margin="0,5,0,0"
                        Foreground="#99FFFFFF"
                        Text="Elapsed" />

                    <TextBlock
                        Grid.Row="3"
                        Grid.Column="2"
                        Margin="0,5,0,0"
                        HorizontalAlignment="Right"
                        Foreground="#FFFFFF"
                        Text="{Binding Elapsed}" />

                </Grid>

            </Grid>

        </Border>
    </Grid>
</UserControl>
